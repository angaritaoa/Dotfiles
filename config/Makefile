install :
	ln -fs $(shell pwd)/bash/bashrc ~/.bashrc
	ln -fs $(shell pwd)/git/gitconfig ~/.gitconfig
	[ -d ~/.config/terminator ] || mkdir -p ~/.config/terminator
	ln -fs $(shell pwd)/terminator/config ~/.config/terminator/config
	cp -fR /mnt/archivos/.ssh ~/
